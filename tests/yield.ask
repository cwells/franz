#
# test yield
#

print("Testing yield...")
errors = 0

loop = fn (n: int)
    for i in 1 to 5
        yield i

try assert loop(5) == [ 1, 2, 3, 4, 5 ] rescue errors++

print(if errors "Some tests failed ({errors}) errors\n" else "passed!\n")
